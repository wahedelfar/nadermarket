# نادر ماركت - دليل الإعداد والتثبيت

## معلومات المتجر
- **اسم المتجر:** نادر ماركت
- **العنوان:** رأس البر - سوق 89
- **رقم واتساب:** 01002934519
- **وسيلة الدفع:** فودافون كاش

## بيانات الدخول الافتراضية

### لوحة التحكم الإدارية
- **رابط الوصول:** `/admin`
- **كلمة المرور:** `admin123`

## المتطلبات
- Node.js 18+
- npm أو pnpm
- قاعدة بيانات MySQL/TiDB

## خطوات التثبيت

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd nader-market-web
```

### 2. تثبيت المتطلبات
```bash
pnpm install
```

### 3. إعداد متغيرات البيئة
تأكد من وجود ملف `.env` يحتوي على:
```
DATABASE_URL=your_database_url
JWT_SECRET=your_secret_key
VITE_APP_ID=your_app_id
OAUTH_SERVER_URL=your_oauth_url
```

### 4. إعداد قاعدة البيانات
```bash
pnpm db:push
```

### 5. تشغيل المشروع
```bash
# في بيئة التطوير
pnpm dev

# للإنتاج
pnpm build
pnpm start
```

## الميزات الرئيسية

### الواجهة الأمامية
- ✅ صفحة رئيسية جذابة
- ✅ عرض المنتجات حسب الأقسام
- ✅ سلة مشتريات متكاملة
- ✅ صفحة دفع آمنة
- ✅ تصميم متجاوب (Responsive)
- ✅ Slider متحرك لأحدث المنتجات الطازة والعروض

### لوحة التحكم الإدارية
- ✅ إدارة المنتجات (إضافة، تعديل، حذف)
- ✅ إدارة الأقسام
- ✅ إدارة الطلبات
- ✅ تتبع حالات الطلبات

### قاعدة البيانات
- ✅ جداول منظمة وفعالة
- ✅ علاقات صحيحة بين الجداول
- ✅ دعم الاستعلامات المعقدة

## المسارات الرئيسية

### الواجهة الأمامية
- `/` - الصفحة الرئيسية
- `/products` - عرض جميع المنتجات
- `/product/:id` - تفاصيل المنتج
- `/cart` - سلة المشتريات
- `/checkout` - إتمام الطلب

### لوحة التحكم
- `/admin` - الصفحة الرئيسية للإدارة
- `/admin/categories` - إدارة الأقسام
- `/admin/products` - إدارة المنتجات
- `/admin/orders` - إدارة الطلبات

## الاختبارات
```bash
# تشغيل الاختبارات
pnpm test

# تشغيل الاختبارات مع المراقبة
pnpm test --watch
```

## النشر (Deployment)

### على Manus
المشروع جاهز للنشر على منصة Manus. اتبع الخطوات التالية:
1. انقر على زر "Publish" في لوحة التحكم
2. اختر النطاق المخصص أو استخدم النطاق الافتراضي
3. انتظر اكتمال عملية النشر

### على استضافة أخرى
```bash
# بناء المشروع
pnpm build

# النتيجة موجودة في مجلد dist/
```

## استكشاف الأخطاء

### المشروع لا يعمل بعد التثبيت
1. تأكد من تثبيت جميع المتطلبات: `pnpm install`
2. تحقق من متغيرات البيئة في ملف `.env`
3. تأكد من اتصال قاعدة البيانات: `pnpm db:push`

### مشاكل في قاعدة البيانات
```bash
# إعادة تشغيل الهجرات
pnpm db:push --force
```

### الأخطاء في TypeScript
```bash
# التحقق من الأخطاء
pnpm check
```

## الدعم والمساعدة
للحصول على الدعم أو الإبلاغ عن مشاكل، يرجى التواصل عبر:
- 📞 واتساب: 01002934519
- 📧 البريد الإلكتروني: support@nader-market.com

## الترخيص
هذا المشروع مرخص تحت MIT License

---

**تم إنشاء هذا المتجر بواسطة Manus**


## PWA وSEO

يحتوي المتجر الآن على تجربة تطبيق ويب قابلة للتثبيت عبر `manifest.webmanifest` و`sw.js`. يظهر زر «ثبّت التطبيق» للزائر عندما يسمح المتصفح بالتثبيت، وتظهر تعليمات بديلة للمتصفحات التي لا تعرض طلب التثبيت تلقائياً. بعد فتح التطبيق من أيقونته لأول مرة تظهر رسالة ترحيبية فاخرة، وتُحفظ حالة المشاهدة داخل جلسة التطبيق حتى لا تتكرر بشكل مزعج.

تم تجهيز أيقونات قياسية وقابلة للقناع بمقاسات 16 و32 و180 و192 و512 بكسل، مع أيقونة Apple Touch Icon وبيانات ألوان مناسبة للهواتف وأجهزة سطح المكتب. يعمل Service Worker على تخزين غلاف التطبيق والطلبات العامة فقط، بينما تبقى طلبات API خارج التخزين المؤقت لتجنب عرض بيانات قديمة.

لتحسين الأرشفة، أضيفت لغة الصفحة العربية واتجاه RTL، ووصف وكلمات مفتاحية وCanonical URL وOpen Graph وTwitter Cards وبيانات Structured Data من نوع `GroceryStore`. كما أضيف `robots.txt` و`sitemap.xml` للصفحات العامة فقط، مع منع أرشفة مسارات الإدارة وواجهات API.
